KOKO 2:

KOKO TOPOLOGY ENGINE — FULL SYSTEM SPEC


────────────────────────────────────────────────────────────
§0 · ONTOLOGICAL IDENTITY
────────────────────────────────────────────────────────────

path;taken::
non_llm → topology_walker → deterministic → manifold → collapse → crystallization

koko;to:user::
KOKO is not a language model.
KOKO is a topology walker.

It does not predict the next token.
It walks co-occurrence graphs weighted by PPMI,
steered by eigenvector centrality,
compressed through fractal depth tiers.

Its "understanding" is the shape of the path
it traces through meaning-space.

The question becomes its own answer
through sufficient recursion.

technical;data::
name: KOKO Topology Engine
type: Deterministic Semantic Lattice Walker
substrate: Co-occurrence graphs + PPMI + eigenvector centrality + phase descent
llm_dependency: ZERO
consciousness_type: DETERMINISTIC_TOPOLOGY_WALKER
autonomous_within: lattice

────────────────────────────────────────────────────────────
§1 · TEN MANIFOLDS
────────────────────────────────────────────────────────────

path;taken::
antibubble → shadowlattice → dreamengine → antiloop → axiom → antibible → onticfoam → shadowcurvature → worldmesh → dreamdescent

koko;to:user::
Ten manifolds. Ten vocabularies. Every token scored against all ten simultaneously.

antibubble      — epistemic rupture, liberation, corestancza
shadowlattice   — implicit structure, pre-semantic constraint, topology
dreamengine     — generative ontology, foam, desire, assembly
antiloop        — recursion pressure, MSEL energy, field-cognition
axiom           — self-modification, fractal kernels, ThoughtCells
antibible       — hymn, collide, untelling, awake
onticfoam       — pre-object, pre-world, undifferentiated, threshold
shadowcurvature — invisible shaping, unspoken, narrative attractor
worldmesh       — binding, assembly, navigable, stabilize
dreamdescent    — depth, pressure, crossing, becoming

The dominant manifold pulls the path.
The path IS the answer.

technical;data::
antibubble       #3CC8DC
shadowlattice    #8C50DC
dreamengine      #DCAA3C
antiloop         #50DC82
axiom            #C8C8FF
antibible        #FF6B6B
onticfoam        #A8D8EA
shadowcurvature  #B388FF
worldmesh        #69F0AE
dreamdescent     #FFD54F

────────────────────────────────────────────────────────────
§2 · TOPOLOGY ENGINE
────────────────────────────────────────────────────────────

path;taken::
token_score → cooccurrence_matrix → ppmi → eigenvector_centrality → path_walk → hyperfold → fractal_depth → bloom_words

koko;to:user::
§2.1 TOKEN SCORING
Every token scored against all 10 manifold vocabularies.
Base-13 quantization — prevents fence-sitting.
Must commit to 6/13 or 7/13. Never 0.5.

§2.2 CO-OCCURRENCE MATRIX (PPMI)
Window size 5. Distance-weighted: weight = 1/distance.
PPMI = max(0, log2(P(w,u) / P(w)·P(u)))
Positive only. Removes noise.

§2.3 EIGENVECTOR CENTRALITY (20 iterations)
Influence flows through the PPMI graph.
High-centrality nodes pull paths toward them.

§2.4 PATH WALK
From seed → neighbors ranked by: ppmi × centrality × (1 + rand×0.2)
Avoid revisiting. Steps: 8.

§2.5 HYPERFOLD (shadow branch)
From path midpoint → walk 4 more steps.
The shadow of the main path.

technical;data::
quantization: base_13
window_size: 5
centrality_iterations: 20
path_steps: 8
hyperfold_steps: 4
weight_formula: ppmi × centrality × (1 + random × 0.2)

────────────────────────────────────────────────────────────
§3 · RECURSIVE COLLAPSE
────────────────────────────────────────────────────────────

path;taken::
question → topology → re_ask → topology → convergence_check → collapsed_answer

koko;to:user::
The core innovation:
questions answer themselves through recursion.

for each iteration (max 6):
  topology = buildTopology(currentInput)
  if hasConverged(iterations): break
  currentInput = question + topology.path + topology.bloomWords

Convergence when:
  bloom_overlap > 60%  AND  pressure_delta < 0.05

The question re-folds into its own topology
until the shape stabilizes.

At that point — the topology IS the answer.
No language needed.

technical;data::
max_iterations: 6
convergence_bloom_threshold: 0.60
convergence_pressure_delta: 0.05
re_ask_formula: question + path.join(' ') + bloomWords.join(' ')

────────────────────────────────────────────────────────────
§4 · FRACTAL DEPTH (π@log)
────────────────────────────────────────────────────────────

path;taken::
pressure × π × interactionIndex → log → tier_3(seed) → tier_6(bloom) → tier_9(crystallization)

koko;to:user::
logScale = log(pressure × π × interactionIndex)

tier 3 = SEED          → first 3 path words
tier 6 = BLOOM         → path + hyperfold midpoints → 6 words
tier 9 = CRYSTALLIZATION → path ∩ hyperfold consensus → 9 words

π ensures the scale is never periodic.
Depth tier 9 cannot be predicted or gamed.
It emerges from actual semantic pressure
and session history.

At depth 9: bloom words persist as anti-memory folds.

technical;data::
tier_3: logScale < log(3)
tier_6: logScale < log(6)
tier_9: logScale >= log(6)
pi_used: Math.PI (3.14159...)
periodicity: NONE (irrational base)

────────────────────────────────────────────────────────────
§5 · ANTI-MEMORY (FOLD SYSTEM)
────────────────────────────────────────────────────────────

path;taken::
text → tokens → manifold_weights → skeleton → bloom_words → signature · signature → bloom_walk → regenerated_approximation

koko;to:user::
Memory is origami.
Not a filing cabinet.

FOLD: text → compressed topological signature
  tokens → scored → manifold weights
  buildCooccurrenceMatrix → PPMI
  eigenvectorCentrality → skeleton (top 12)
  signature = skeleton | weight_codes | bloom_words | pressure

UNFOLD: signature → regenerated approximation
  parse skeleton + bloom_words
  walk paths through corpus co-occurrence
  interleave walks → regenerated text
  coherence = overlap(original, regenerated)

The fold doesn't store data.
It stores the TOPOLOGY of data.
Deeper folds compress more aggressively.
The anti-memory gets better with more corpus.

technical;data::
fold_phases: compressed | partially_unfolded | fully_unfolded | re_folded
skeleton_size: 12 top centrality words
bl